import { heroImages } from '../data/heroImages';
import { CardCarousel } from '../components/CardCarousel';
import { resources } from '../data/resources'; import { Button, Container, Eyebrow, PageHero, Section } from '../components/ui'; import { Seo } from '../components/Seo'; import { ResourceCard } from '../components/cards';
import { QuickLeadSection } from '../features/quickLead/QuickLeadSection';
import { breadcrumbSchema, itemListSchema } from '../lib/structuredData';
export default function Resources(){const schema=[breadcrumbSchema([{name:'Accueil',path:'/'},{name:'Ressources',path:'/ressources'}]),itemListSchema(resources.map(item=>({name:item.title,path:`/ressources/${item.slug}`})))];return <><Seo title="Guides de performance énergétique pour les entreprises" description="Guides pratiques sur les CEE, la GTB, l’air comprimé, le froid, la récupération de chaleur et la déstratification des bâtiments professionnels." canonicalPath="/ressources" image={resources[0].image} imageAlt={resources[0].imageAlt} schema={schema}/><PageHero eyebrow="Guides pratiques" title="Comprendre vos installations avant de décider." text="Des explications structurées, des listes de vérification et des sources publiques pour préparer un projet énergétique professionnel." image={heroImages.architecture} imageAlt="Architecture d’un bâtiment professionnel"/><Section><Container><div className="featured-article"><img src={resources[0].image} alt={resources[0].imageAlt}/><div><Eyebrow>À la une · {resources[0].category}</Eyebrow><h2>{resources[0].title}</h2><p>{resources[0].excerpt}</p><Button to={`/ressources/${resources[0].slug}`}>Lire le guide</Button></div></div><CardCarousel label="Nos guides">{resources.slice(1).map(item=><ResourceCard item={item} key={item.slug}/>)}</CardCarousel></Container></Section><QuickLeadSection variant="resource"/></>}
