import { heroImages } from '../data/heroImages';
import { useParams } from 'react-router-dom'; import { resources } from '../data/resources'; import { Breadcrumb, PageHero, Button, Container } from '../components/ui'; import { Seo } from '../components/Seo'; import NotFound from './NotFound';
export default function ResourceDetail(){const {slug}=useParams();const item=resources.find(entry=>entry.slug===slug);if(!item)return <NotFound/>;return <><Seo title={item.title} description={item.excerpt} noindex={!item.indexable}/><PageHero breadcrumb={item.category} eyebrow={item.category} title={item.title} text={item.excerpt} image={heroImages.architecture}/><article className="article"><Container><div className="article-content"><div><h2>Observer avant de choisir une solution</h2><p>Un projet de performance commence par les usages et les conditions réelles d’exploitation.</p><div className="article-callout"><h3>Votre entreprise est concernée&nbsp;?</h3><Button to="/eligibilite">Vérifier mon éligibilité</Button></div></div></div></Container></article></>}
