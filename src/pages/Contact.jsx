import { useEffect } from 'react'; import { Mail, MapPin, Phone, Sun } from 'lucide-react'; import { siteConfig } from '../config/siteConfig'; import { trackEvent } from '../services/analyticsService'; import { Breadcrumb, Container, Eyebrow } from '../components/ui'; import { Seo } from '../components/Seo'; import { ContactForm } from '../features/contact/ContactForm';
export default function Contact(){useEffect(()=>{trackEvent('contact_started',{});},[]);const phoneHref=`tel:${siteConfig.contact.phone.replace(/\s/g,'')}`;return <><Seo title="Contact" description="Contactez Maîtrise Énergie pour parler de votre site."/><main className="contact-page"><Container><Breadcrumb current="Contact"/><div className="contact-layout"><div><Eyebrow>Contact</Eyebrow><h1>Parlons de votre site.</h1><p className="lead">Expliquez-nous vos enjeux. Nous reviendrons vers vous pour préciser les premières étapes de l’étude.</p><div className="contact-details"><a href={`mailto:${siteConfig.contact.email}`} onClick={()=>trackEvent('email_clicked',{channel:'email'})}><Mail/><span>Email<small>{siteConfig.contact.email}</small></span></a><a href={phoneHref} onClick={()=>trackEvent('phone_clicked',{channel:'contact'})}><Phone/><span>Téléphone<small>{siteConfig.contact.phone}</small></span></a><div><MapPin/><span>Adresse<small>{siteConfig.contact.address}</small></span></div><div><Sun/><span>Disponibilités<small>{siteConfig.contact.hours} · {siteConfig.contact.serviceArea}</small></span></div></div></div><div className="contact-form"><ContactForm/></div></div></Container></main></>}
